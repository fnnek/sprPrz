<% provide(:title, @title) %>
<div class="row">
  <h1>Obserwowani użytkownicy</h1>
  <div class="col-md-8">
    <% if @users.any? %>
        <ul class="users follow">
          <%= render @users %>
        </ul>
      <%= will_paginate %>
    <% end %>

  </div>
</div>